# csc413-project

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

python3 src/create_dataset.py  # download dataset and perform preprocessing
./run-cnn.sh                   # run the CNN model based off ResNet50
./run-transformer.sh           # run the transformer model based off ViT (Vision Transformer)
```
